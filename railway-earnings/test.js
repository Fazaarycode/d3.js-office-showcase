function callReduce(arr1, arr2){
//     arr1.map( (val, index) => {
//         // console.log(index)
//         // console.log(val.label);
//         if(index == 2) {
//             console.log(arr2)
//         }
//     })
    
 }


// var sample = [...dataFromJSON.data , ...dataFromJSON,fields]
    // var fields = dataFromJSON.fields
    // var data = dataFromJSON.data
    // callReduce(fields,data)

    // // var combine  = [...fields, ...data]
    // // console.log(combine);
// })